Update the appropriate JSON files in the data\application-queues folder.

Check if all of the roles are still open and job URLs are still active. Delete the ones which are closed or you are unsure if they are open/closed. Delete the sections directly in the file.

Find each role's latest published/reposted date, searching again for the roles missing one, and sort the table by Posting Date, newest to oldest. Delete any role which is over a month old or replace it with a recent repost of that or similar role in that company.
